[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fixture-build-failure-project"
version = "0.4.0"
description = "Fixture project for intentional build failure coverage."
readme = "README.md"
requires-python = ">=3.12"
